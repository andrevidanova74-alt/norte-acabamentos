/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

export default function App() {
  // The landing page is implemented directly in index.html as a single-file premium landing page.
  // This React component is kept for compatibility but doesn't render anything to avoid conflicts.
  return null;
}
